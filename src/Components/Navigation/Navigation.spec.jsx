import { logRoles, screen } from "@testing-library/dom";
import Navigation from "./Navigation";
import { render } from "@testing-library/react";

describe("Navigation", () => {
  it("should render navigation and your elements", () => {
    const nav = render(<Navigation />);

    
  });
});
